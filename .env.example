# Backend (Render) — variables de entorno
# Copiar a .env y rellenar con valores reales. .env nunca se commitea.

RENDER_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openai/gpt-oss-20b:free

DATABASE_URL=
MEETSCRIBE_BACKEND_URL=http://localhost:3000
